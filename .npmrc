registry=https://registry.npmjs.org/

# 让 pnpm 在不同架构机器上都能正常安装 rollup 的平台二进制可选依赖
# （例如 VitePress/Vite 使用的 rollup 会按 cpu/os 选择 @rollup/rollup-<platform> 包）
supportedArchitectures.cpu=arm64,x64
supportedArchitectures.os=linux,darwin,win32
